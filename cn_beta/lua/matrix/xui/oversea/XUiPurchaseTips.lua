---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2020/7/31 16:37
---

local this = XLuaUiManager.Register(XLuaUi, "UiPurchaseTips")

function this:OnAwake()
    XUiHelper.RegisterClickEvent(self, self.BtnConfirm, self.OnConfirm)
    XUiHelper.RegisterClickEvent(self, self.BtnClose, self.OnCancel)
    XUiHelper.RegisterClickEvent(self, self.BtnTanchuangClose, self.OnClose)
end

function this:OnConfirm()
    XSaveTool.SaveData("AGE_OVER_20", 1)
    if self.ConfirmCallback then
        self.ConfirmCallback()
    end
    self:OnClose()
end

function this:OnClose()
    XLuaUiManager.Close("UiPurchaseTips")
end

function this:OnCancel()
    if self.CancelCallback then
        self.CancelCallback()
    end
    self:OnClose()
end

function this:OnStart(confirmCb, cancelCb)
    self.ConfirmCallback = confirmCb
    self.CancelCallback = cancelCb
end